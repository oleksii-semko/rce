import React, { useContext } from "react";
import { AppContext } from "../context";

/**
 * Display component
 */
const PageDisplay = () => {
  return (
    <div></div>
  );
}

export default PageDisplay;
